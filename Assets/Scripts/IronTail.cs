using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class IronTail : MonoBehaviour
{
    int uses = 5;
    int power = 100;
    int accuracy = 75;
    GameManager.Type type = GameManager.Type.NORMAL;

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
