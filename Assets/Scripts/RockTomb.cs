using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class RockTomb : MonoBehaviour
{
    int uses = 20;
    int power = 60;
    int accuracy = 95;
    GameManager.Type type = GameManager.Type.NORMAL;

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
